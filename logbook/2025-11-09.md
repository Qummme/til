---
date: 2025-11-09
tags: [server]
---

## 💡logbookはlogっぽく残したらよいのでは #idea

- あまり校正せず、もっとlogっぽく残してもいいのでは
- zennのスクラップみたいに

## 💻WSL Ubuntuの起動が遅いエラーの調査 #server

- wslのzsh起動時に毎回以下エラー
```sh
wsl: Failed to start the systemd user session for (略). See journalctl for more details.
```
- `journalctl -f`で見ると以下エラーが見つかる
```sh
WSL (2 - init-systemd(Ubuntu)) ERROR: WaitForBootProcess: /sbin/init failed to start within 10000ms
```
- ChatGPTに聞く
- `sudo systemd-analyze blame`で起動が遅いサービスを調べる
```sh
2min 156ms systemd-networkd-wait-online.service
2.908s postgresql@14-main.service
```
- wslだと`systemd-networkd-wait-online.service`は止めても問題ないっぽいので止める
    - 物理NICを探してるらしい
- postgresqlも普段は使ってないので止める
```sh
$ sudo systemctl disable systemd-networkd-wait-online.service
$ sudo systemctl disable postgresql
```
- tips: systemctl maskでstartもできないようにすることも可能(今回はしないけど)


